# -------------------------------------------------------
# Retry mínimo para testes ? evita espera desnecessária
# -------------------------------------------------------
kafka.retry.max-attempts=1
kafka.retry.initial-interval-ms=100
kafka.retry.multiplier=1.0
kafka.retry.max-interval-ms=100
